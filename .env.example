# Chave OpenAI — obrigatória para o chat GPT no guia (local e Vercel).
OPENAI_API_KEY=sk-...

# Backend local (opcional)
# PORT=3001

# Frontend — em produção no mesmo domínio da Vercel, deixe vazio.
# VITE_API_URL=
